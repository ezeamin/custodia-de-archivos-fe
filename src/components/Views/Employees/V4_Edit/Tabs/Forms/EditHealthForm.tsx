import { EmployeeInfoProps } from '@/components/interface/views';

const EditHealthForm = (props: EmployeeInfoProps) => {
  const { data } = props;

  console.log(data);

  return <div>EditHealthForm</div>;
};
export default EditHealthForm;
