import { EditDocumentsFormProps } from '@/components/interface/views';

const EditDocumentsForm = (props: EditDocumentsFormProps) => {
  const { data } = props;

  console.log(data);

  return <div>EditDocumentsForm</div>;
};
export default EditDocumentsForm;
